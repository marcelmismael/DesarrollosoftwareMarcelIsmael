// Entorno de PRODUCCION. Se usa con `ng build --configuration production`.
// IMPORTANTE: reemplaza esta URL por la URL publica real del backend
// una vez desplegado en Render / Railway / VPS (Actividad 9 - Despliegue).
// Ejemplo: 'https://mayte-api.onrender.com/tickets'
export const environment = {
  production: true,
  apiUrl: 'https://desarrollosoftwaremarcelismael.onrender.com/tickets'
};